# Email Configuration
EMAIL_SMTP_SERVER=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=your-email@gmail.com
EMAIL_PASSWORD=your-app-password
EMAIL_SENDER_EMAIL=your-email@gmail.com
EMAIL_SENDER_NAME=Space Force Visitor Portal

# Database Configuration
DB_SERVER=127.0.0.1
DB_PORT=5432
DB_DATABASE=sf-visitor-management
DB_USER=postgres
DB_PASSWORD=your-db-password

# Note: For Gmail, you need to use an App Password, not your regular account password.
# To create an App Password:
# 1. Enable 2-Step Verification on your Google Account
# 2. Go to https://myaccount.google.com/apppasswords
# 3. Select "Mail" as the app and "Other" as the device (name it "VisitorPortal")
# 4. Copy the generated 16-character password (4 groups of 4 characters)